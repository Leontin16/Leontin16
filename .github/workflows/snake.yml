name: generate animation

on:
  # Запуск каждые 24 часа
  schedule:
    - cron: "0 0 * * *" 
  
  # Возможность запустить вручную
  workflow_dispatch:
  
  # Запуск при каждом пуше в main ветку
  push:
    branches:
    - main

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    
    steps:
      # Генерация змейки из графиков вкладов пользователя Leontin16
      - name: generate github-contribution-grid-snake.svg
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: Leontin16
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark
          
      # Пуш сгенерированных файлов в ветку output
      - name: push github-contribution-grid-snake.svg to the output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
